{
    "name": "{{.ProjectName}}",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "aerostack dev",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "@aerostack/sdk": "^0.7.0",
        "hono": "^4.0.0"
    },
    "devDependencies": {
        "@cloudflare/vitest-pool-workers": "^0.7.0",
        "vitest": "~3.0.0"
    }
}