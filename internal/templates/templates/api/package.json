{
  "name": "{{.ProjectName}}",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aerostack/sdk": "^0.2.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.0",
    "esbuild": "^0.24.0",
    "vitest": "~3.0.0"
  }
}